groups:
  - name: master
    jobs:
      - management-dev
      - management
  - name: pull-request
    jobs:
      - jmx-exporter-pr
  - name: mirror
    jobs:
      - mirror-dwpdigital-jmx-exporter-dev
      - mirror-dwpdigital-jmx-exporter
